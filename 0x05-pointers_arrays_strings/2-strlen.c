#include "main.h"

/**
 * _strlen - string length
 * @s : parameter s
 * Return: return the string length
 */

int _strlen(char *s)
{
int s_length;

for (s_length = 0; s[s_length] != '\0'; s_length++)
{
}

return (s_length);
}
