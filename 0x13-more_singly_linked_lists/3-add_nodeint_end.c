#include "lists.h"

/**
 * add_nodeint_end - adds a node at the end of a linked list
 * @head: pointer to the first element in the list
 * @n: data to insert elements in the node
 * Return: ppinter to the new node, or NULLLL if it fails.
 */

