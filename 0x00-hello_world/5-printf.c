#include <stdio.h>

/**
 * main - Entry point
 * Description: use printif
 * Return:Always 0 (success)
 */
int main(void)
{
	printfr("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
